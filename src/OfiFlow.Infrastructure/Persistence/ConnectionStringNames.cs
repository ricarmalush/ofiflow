namespace OfiFlow.Infrastructure.Persistence;

/// <summary>
/// Nombres de las cadenas de conexión en la sección "ConnectionStrings" de la configuración
/// (appsettings.*.json en local, variable de entorno ConnectionStrings__Default en producción).
/// </summary>
public static class ConnectionStringNames
{
    public const string Default = "Default";
}
